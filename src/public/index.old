let user = "";
//const socket = io();

Swal.fire({
  title: "Administrador de productos",
  //input: "text",
  text: "Para ver la lista de productos ingrese a la ruta /products",
  /*
  inputValidator: (values) => {
    return !values.trim() && "Por favor ingrese su nombre";
  },*/
}); /*.then((result) => {
  user = result.value;
  //socket=io()
});*/
